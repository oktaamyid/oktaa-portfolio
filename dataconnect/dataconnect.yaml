specVersion: "v1beta"
serviceId: "portfo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "portfo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
